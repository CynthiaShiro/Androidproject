<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.donationproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/blood"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/blood"
        android:supportsRtl="true"
        android:theme="@style/Theme.DonationProject">
        <activity
            android:name=".RecipientActivity"
            android:exported="true" />
        <activity
            android:name=".DonorActivity"
            android:exported="true"
            android:label="@string/title_activity_donor"
            android:theme="@style/Theme.DonationProject.NoActionBar" />
        <activity
            android:name=".HomeActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>